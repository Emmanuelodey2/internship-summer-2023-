const name = document.getElementById("name");
const state = document.getElementsByClassName("state")[2];

name.style.display = "none";
name.innerHTML = "Emmanuel is the best ";
state.innerHTML = " phase 1";

state.style.backgroundColor = "red";

// event are  action based response
name.addEventListener(click, () => {
console.log("John terry")}
)
